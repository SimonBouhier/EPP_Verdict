Plan d'Exécution — Audit Solana Directives 1-5

Pour : Claude Code
Contexte : Audit de la couche Solana post-diagnostic solana_diagnostic.log 2026-02-23.
D1 validé en exploration : Les 4 méthodes RPC ont des corps réels — submit_attestation, get_attestation, query_by_claim, query_by_subject. Aucun correctif requis sur ce point.


ÉTAPE 0 — Baseline (obligatoire, première commande)
bashpytest tests/ --tb=short -q 2>&1 | tail -3
Relever le chiffre exact. La livraison finale devra montrer baseline → baseline + delta.

ÉTAPE 1 — D5 : Grep résiduel avant suppression (obligatoire avant toute modification)
Un désalignement Python↔Rust est confirmé : bridge.py contient 3 aliases backward compat ("low"→0, "medium"→1, "high"→2) absents du match Rust. Tout appel live avec ces valeurs retourne EppError::InvalidConfidenceTier côté Solana.
Avant de toucher au code, prouver qu'aucun appelant n'émet ces valeurs :
bashgrep -rn '"low"\|"medium"\|"high"' \
  services/ pipeline/ tests/ \
  --include="*.py" \
  | grep -v "CONFIDENCE_TIER_MAP\|backward\|# "
Si 0 résultats → procéder à l'étape 2.
Si résultats → lister les appelants ici et les migrer vers les valeurs primaires avant suppression. Ne pas continuer.

ÉTAPE 2 — D5 : Suppression backward compat + test bijection
2a — Modifier services/solana/bridge.py
Supprimer les 3 lignes backward compat et leur commentaire dans CONFIDENCE_TIER_MAP. Le mapping final doit contenir exactement 4 clés : sandbox, proposition, validated, verified.
2b — Ajouter dans tests/test_bridge_solana_compat.py
Un test de bijection stricte Python↔Rust :
pythondef test_confidence_tier_map_bijection_with_rust():
    """Vérifie que CONFIDENCE_TIER_MAP est en bijection stricte avec confidence_tier_to_u8 Rust."""
    rust_values = {"sandbox", "proposition", "validated", "verified"}
    python_keys = set(CONFIDENCE_TIER_MAP.keys())
    assert python_keys == rust_values, (
        f"Désalignement Py↔Rust.\n"
        f"Clés Python orphelines : {python_keys - rust_values}\n"
        f"Clés Rust orphelines : {rust_values - python_keys}"
    )
    # Vérifier aussi la bijection des valeurs (0-3 sans trous)
    assert sorted(CONFIDENCE_TIER_MAP.values()) == [0, 1, 2, 3]
Vérification intermédiaire :
bashpytest tests/test_bridge_solana_compat.py -v -k "bijection"

ÉTAPE 3 — D7 : Refactor guard _SOLANA_AVAILABLE
Le guard actuel dans test_phase4_solana.py est fonctionnellement correct (pytest.skip() explicite, non silencieux) mais incohérent avec le style de test_phase1_client.py.
Modifier tests/test_phase4_solana.py : convertir le guard inline en décorateur de classe :
python@pytest.mark.skipif(not _SOLANA_AVAILABLE, reason="solana-py not installed")
class TestSolanaClientReal:
    ...
Supprimer le bloc if not _SOLANA_AVAILABLE: pytest.skip(...) devenu redondant à l'intérieur de la classe.
Vérifier aussi que tests/test_phase1_client.py n'importe pas _SOLANA_AVAILABLE sans l'utiliser. Si l'import est inutilisé, le supprimer.

ÉTAPE 4 — D6 : Program ID dans README.md
Le Program ID est déjà présent dans ARCHITECTURE.md (l.189). L'ajouter dans README.md, section Solana :
markdown**Program ID (devnet/localnet)** : `98Fc2oL2cKsTDGYi3GifggzkQkEQSRn2oTgg8HsaVa3C`
Statut : déployé devnet (Anchor 0.32.1) — à remplacer avant mainnet.

ÉTAPE 5 — D5b : Levée des AUDIT_REQUIRED validés
Trois marqueurs dont la condition est désormais vérifiée. Les remplacer :
FichierLigneRemplacementservices/solana/client.py474# AUDIT_CLEARED 2026-02-23 — CLAIM_HASH_OFFSET=41 vérifié vs state.rs (disc8+bump1+submitter32)services/solana/client.py511# AUDIT_CLEARED 2026-02-23 — SUBJECT_OFFSET=73 vérifié vs state.rs (disc8+bump1+submitter32+hash32)programs/epp/src/lib.rs113/// AUDIT_CLEARED 2026-02-23 — PDA seeds [b"attestation", submitter, claim_hash] vérifiés vs client.py:derive_attestation_pda
Les autres AUDIT_REQUIRED présents dans le codebase ne sont pas concernés — ne pas les toucher.

ÉTAPE 6 — Vérifications finales (toutes obligatoires)
bash# 1. Plus aucun alias backward compat
grep -rn '"low"\|"medium"\|"high"' services/solana/ --include="*.py"
# → 0 résultats

# 2. Marqueurs de clôture en place
grep -n "AUDIT_CLEARED 2026-02-23" services/solana/client.py programs/epp/src/lib.rs
# → 3 lignes

# 3. Régression complète
pytest tests/ --tb=short -q 2>&1 | tail -5
# → 0 failed, baseline+delta passed

Récapitulatif des fichiers modifiés
FichierModificationservices/solana/bridge.pySupprimer 3 lignes backward compat + commentairetests/test_bridge_solana_compat.pyAjouter test_confidence_tier_map_bijection_with_rusttests/test_phase4_solana.pyGuard inline → décorateur @pytest.mark.skipiftests/test_phase1_client.pySupprimer import _SOLANA_AVAILABLE inutilisé (si confirmé)README.md2 lignes Program ID section Solanaservices/solana/client.py2 AUDIT_CLEARED (l.474, l.511)programs/epp/src/lib.rs1 AUDIT_CLEARED (l.113)


Rappel protocole : Fournir la sortie de l'étape 0 (baseline) et de l'étape 1 (grep résiduel) avant d'envoyer les diffs. Ces deux sorties sont des prérequis, pas des vérifications optionnelles.