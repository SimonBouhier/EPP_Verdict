node_modules/
dist/
coverage/
.vite/
*.log
*.local
.DS_Store
.env
.env.local

# public/data/ is committed: Vercel can't access ../demos/ outside the
# Root Directory in monorepo mode, so we ship the JSONs as build inputs.
# Locally, copy-data.mjs refreshes them from demos/benchmark_runs/ on
# every npm run dev / build — commit the result when you add scenarios.
